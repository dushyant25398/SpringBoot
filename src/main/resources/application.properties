server.port=8085
# database configuration:mysql

# now to add usl  of database.

spring.datasource.url=jdbc:mysql://localhost:3306/library
# my hiber is the name of mysql database.

# now will create a user
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# if we are using old version then no need to add cj in the driver name

# Hibernate configuration (Jpa is bene implmented by hhibernate  , so we need to configure hibernate)

spring.jpa.hibernate.ddl-auto=create
#(with the help of this our tabble would be automatically created. )
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect


